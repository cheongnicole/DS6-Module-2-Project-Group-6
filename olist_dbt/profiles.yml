olist_dbt:
  target: dev
  outputs:
    # Staging: Rename columns, Cast data types, Trim strings, Standardize nulls and blanks
    dev:
      dataset: olist_dev
      job_execution_timeout_seconds: 300
      job_retries: 1
      # keyfile: /Users/zanelim/Downloads/personal/secret/meltano-learn-03934027c1d8.json # Use your path of key file
      location: US
      # method: service-account
      method: oauth
      priority: interactive
      project: project-7781a5d3-3f4e-4cd5-a43 # enter your google project id
      threads: 1
      type: bigquery

elementary:
  outputs:
    default:
      type: "bigquery"
      project: "project-7781a5d3-3f4e-4cd5-a43"
      dataset: "olist_dev_elementary"
      method: "oauth"  # Configure your auth method and add the required fields according to https://docs.getdbt.com/reference/warehouse-setups/bigquery-setup#authentication-methods
      threads: 1